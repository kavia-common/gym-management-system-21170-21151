/**
 * PUBLIC_INTERFACE
 * Card adapter - Re-export Design Things Card to consolidate usage.
 * Migration note: Replaces legacy Card with DT adapter.
 */
import { Card as DTCard } from "../design-system/ui";
export default DTCard;
