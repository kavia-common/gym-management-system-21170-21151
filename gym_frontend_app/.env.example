# Required by Supabase client in src/lib/supabaseClient.ts
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_KEY=your-anon-public-key

# Social OAuth providers are disabled by default.
# To re-enable later:
#  - Add Google/GitHub provider vars back (e.g., REACT_APP_GOOGLE_CLIENT_ID, REACT_APP_GITHUB_CLIENT_ID, and their redirect URIs)
#  - Reintroduce the social buttons in the auth UI if they were stubbed out
# See src/lib/README.md for details.
